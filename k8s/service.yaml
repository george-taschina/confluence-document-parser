apiVersion: v1
kind: Service
metadata:
  name: straion-import-service
  labels:
    app: straion-import-service
spec:
  type: LoadBalancer
  selector:
    app: straion-import-service
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  sessionAffinity: None
